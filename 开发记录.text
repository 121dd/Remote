被控机执行命令

客户端（主动，控制方）和服务器（被动，被控方）的网络打通了

客户端发一个命令给服务器
cmd == 1: 服务器收到后解析执行，将屏幕数据通过网络返回给客户端，客户端拿到数据后解析显示 

1/搭建服务器处理命令的框架

2/服务器处理请求屏幕响应

3/客户端改成窗口
连接器入口点：WinMainCRTStartup 高级：子系统：窗口

4.客户端解析显示图片
5.调试客户端解析显示图片BUG

6.调试BUG--从功能流程去调试
然后再断打印调试

7.服务器实现鼠标和键盘命令控制
（1）客户端获取捕获键盘信息
（2）打包这些信息发给服务器
（3）服务然后模拟执行鼠标操作

（1）客户端获取捕获键盘信息
比如按下一个A键---然后被映射为一个数字-----虚拟键码
（2）打包这些信息发送给键盘
（3）服务器模拟执行键盘操作
服务器拿到虚拟键码去模拟执行操作